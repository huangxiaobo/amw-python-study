# /usr/bin/env python# -*- encoding=utf-8 -*-import collectionsdef Enum(*values, **kwargs):    names = kwargs.get('names')    if names:        return collections.namedtuple('Enum', names)(*values)    return collections.namedtuple('Enum', values)(*values)TESTRUN_STATUSES = Enum(    'stopped',    'restarted',    'finished',    'running')TESTRUN_STATUSES_WITH_NAMES = Enum(    'stopped',    'finished',    'running',    names=['x', 'y', 'z'])print TESTRUN_STATUSES_WITH_NAMESTEST_STATUSES = Enum(    'stopped',    'restarted',    'finished',    'running',    'error',    'skipped',    'success',    'failure',    'wait_running',    'disabled')